@Library('java-node') _


pipeline {
    agent {
        label('master')
    }
    
    
   stages {
        stage('Read properties and checkout') {
            steps {

                    WBuild()

            }
        }
    }
}
